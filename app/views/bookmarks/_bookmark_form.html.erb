<% if current_user.bookmarked? recipe %>
  <div id="ajax-bookmark-unbookmark"><%= render "bookmarks/unbookmark", recipe: recipe, bookmark: bookmark %></div>
<% else %>
  <div id="ajax-bookmark-unbookmark"><%= render "bookmarks/bookmark", recipe: recipe %></div>
<% end %>
